<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Calendar.Views.Components.ToolBarComponent"
             x:Name="This">

    <ContentView.Content>
        <Frame>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ImageButton Grid.Column="0" 
                             Source="arrow_left.png"
                             Padding="10"
                             Command="{Binding BackCommand, Source={x:Reference This}}"/>
                <Label Grid.Column="1"
                       IsVisible="{Binding IsCentralTitleVisible, Source={x:Reference This}}"
                       Text="{Binding CentralTitle, Source={x:Reference This}}"
                       Style="{StaticResource HeaderLabel}"/>
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>