<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Calendar.Views"
             mc:Ignorable="d"
             x:Class="Calendar.Views.SettingsView"
             Style="{StaticResource ContentPageStyle}">

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Frame Grid.Row="0"/>

            <Grid Grid.Row="1">
                <Label Grid.Column="0" Text="Разноцветный стиль" Style="{StaticResource ListViewLabel}" HorizontalOptions="Start"/>
                <CheckBox Grid.Column="1" IsChecked="{Binding IsColorsOn}" HorizontalOptions="Start"/>
            </Grid>

            <Frame Grid.Row="3" 
                   Padding="10">
                <local:MainNavigationView
                    LeftCommand="{Binding PrevMonthCommand}" 
                    HouseCommand="{Binding MonthViewCommand}" 
                    RightCommand="{Binding NextMonthCommand}"/>
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage>